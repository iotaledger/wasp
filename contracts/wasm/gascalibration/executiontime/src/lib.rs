// Copyright 2020 IOTA Stiftung
// SPDX-License-Identifier: Apache-2.0

// (Re-)generated by schema tool
// >>>> DO NOT CHANGE THIS FILE! <<<<
// Change the json schema instead

#![allow(dead_code)]
#![allow(unused_imports)]

use executiontime::*;
use wasmlib::*;

use crate::consts::*;
use crate::params::*;
use crate::results::*;
use crate::state::*;

mod consts;
mod contract;
mod params;
mod results;
mod state;

mod executiontime;

const EXPORT_MAP: ScExportMap = ScExportMap {
    names: &[
    	FUNC_F,
	],
    funcs: &[
    	func_f_thunk,
	],
    views: &[
	],
};

#[no_mangle]
fn on_call(index: i32) {
	ScExports::call(index, &EXPORT_MAP);
}

#[no_mangle]
fn on_load() {
    ScExports::export(&EXPORT_MAP);
}

pub struct FContext {
	params: ImmutableFParams,
	results: MutableFResults,
	state: MutableexecutiontimeState,
}

fn func_f_thunk(ctx: &ScFuncContext) {
	ctx.log("executiontime.funcF");
	let f = FContext {
		params: ImmutableFParams { proxy: params_proxy() },
		results: MutableFResults { proxy: results_proxy() },
		state: MutableexecutiontimeState { proxy: state_proxy() },
	};
	ctx.require(f.params.n().exists(), "missing mandatory n");
	func_f(ctx, &f);
	ctx.results(&f.results.proxy.kv_store);
	ctx.log("executiontime.funcF ok");
}

package logger

/*
import (
	"github.com/iotaledger/hive.go/core/app"
)

type ParametersLogger struct {
	Level             string   `default:"debug" usage:"the log level"`
	DisableCaller     bool     `default:"false" usage:"disables the call trace"`
	DisableStacktrace bool     `default:"true" usage:"disables the stacktrace"`
	Encoding          string   `default:"console" usage:"sets the encoding"`
	OutputPaths       []string `default:"['stdout', 'wasp.log']" usage:"defines the outputs"`
	DisableEvents     bool     `default:"true" usage:"disables events"`
}

var ParamsLogger = &ParametersLogger{}

var params = &app.ComponentParams{
	Params: map[string]any{
		"logger": ParamsLogger,
	},
	Masked: nil,
}
*/

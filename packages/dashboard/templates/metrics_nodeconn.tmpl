{{define "title"}}Connection to L1 metrics{{end}}

{{define "body"}}
<div class="card fluid">
	<h2 class="section">Chains</h2>
	<ul>
		{{range $_, $c := .Chains}}
			{{ $id := $c.ChainRecord.ChainID.Base58 }}
			<li><a href="{{ uri "chain" $id }}"><code>{{ $id }}</code></a></li>
		{{end}}
	</ul>
</div>
{{end}}

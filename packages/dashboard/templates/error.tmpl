{{define "title"}}Error{{end}}

{{define "body"}}
<div class="card fluid error">
	<div class="section">
		<h2>{{.Code}} - {{.StatusText}}</h2>
		<p><code>{{.Message}}</code></p>
	</div>
</div>
{{end}}

query GetAllCoins(
	$owner: IotaAddress!,
	# @genqlient(pointer: true)
	$first: Int,
	# @genqlient(pointer: true)
	$cursor: String,
) {
	address(address: $owner) {
		address
		coins(first: $first, after: $cursor) {
			pageInfo {
				hasNextPage
				endCursor
			}
			nodes {
				coinBalance
				contents {
					type {
						repr
					}
				}
				address
				version
				digest
				previousTransactionBlock {
					digest
				}
			}
		}
	}
}
